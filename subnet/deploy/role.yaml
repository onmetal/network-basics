apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  name: gardener-subnet-controller
rules:
- apiGroups:
  - core.gardener.cloud
  resources:
  - subnets
  verbs:
  - '*'

